function _0x3453() { const _0x2b3bb8 = ['3326920AZktxB', '1405185SMgqGD', '14708538CCsCnc', '2595nxCAXK', '6ZUczOW', '532450IazyOK', '3345622hHkDlX', '471325JicfpR', '1388jnwMlv', 'https://discord.gg/Cr2j38SQM7']; _0x3453 = function () { return _0x2b3bb8; }; return _0x3453(); } const _0x14b67e = _0x227e; function _0x227e(_0x7ef7cb, _0x2f9f67) { const _0x345352 = _0x3453(); return _0x227e = function (_0x227e50, _0x103cb0) { _0x227e50 = _0x227e50 - 0x1d2; let _0x284d09 = _0x345352[_0x227e50]; return _0x284d09; }, _0x227e(_0x7ef7cb, _0x2f9f67); } (function (_0x329f2d, _0x976660) { const _0x1a9d58 = _0x227e, _0x42a685 = _0x329f2d(); while (!![]) { try { const _0x13a78c = -parseInt(_0x1a9d58(0x1da)) / 0x1 + -parseInt(_0x1a9d58(0x1d8)) / 0x2 + -parseInt(_0x1a9d58(0x1d6)) / 0x3 * (parseInt(_0x1a9d58(0x1db)) / 0x4) + -parseInt(_0x1a9d58(0x1d4)) / 0x5 + parseInt(_0x1a9d58(0x1d7)) / 0x6 * (-parseInt(_0x1a9d58(0x1d9)) / 0x7) + parseInt(_0x1a9d58(0x1d3)) / 0x8 + parseInt(_0x1a9d58(0x1d5)) / 0x9; if (_0x13a78c === _0x976660) break; else _0x42a685['push'](_0x42a685['shift']()); } catch (_0x1d86e9) { _0x42a685['push'](_0x42a685['shift']()); } } }(_0x3453, 0x3de13)); const stringTyped = 'byteclub', linkToWhere = _0x14b67e(0x1d2);
function _0x508c(_0x5c1ef0, _0x3dca98) { const _0x203243 = _0x2032(); return _0x508c = function (_0x508cd2, _0x44b843) { _0x508cd2 = _0x508cd2 - 0x1b1; let _0x183ae8 = _0x203243[_0x508cd2]; return _0x183ae8; }, _0x508c(_0x5c1ef0, _0x3dca98); } const _0x4e57f9 = _0x508c; function _0x2032() { const _0x176a5e = ['678rFScvL', '973145dPpJgu', '2848820jVlAIB', 'ArrowUpArrowUpArrowDownArrowDownArrowLeftArrowRightArrowLeftArrowRightba', '6qeleye', '705752DZSglc', '132652etzCOD', 'Konami\x20mode\x20activated', '14lFauMN', '2132523NluOlP', '449884VDHYej', '615yPFhSm']; _0x2032 = function () { return _0x176a5e; }; return _0x2032(); } (function (_0x50ff1d, _0x430217) { const _0x46020a = _0x508c, _0x5e0dea = _0x50ff1d(); while (!![]) { try { const _0x3f9135 = parseInt(_0x46020a(0x1ba)) / 0x1 + parseInt(_0x46020a(0x1bc)) / 0x2 * (-parseInt(_0x46020a(0x1bb)) / 0x3) + parseInt(_0x46020a(0x1b6)) / 0x4 + -parseInt(_0x46020a(0x1b1)) / 0x5 * (-parseInt(_0x46020a(0x1b4)) / 0x6) + -parseInt(_0x46020a(0x1b8)) / 0x7 * (-parseInt(_0x46020a(0x1b5)) / 0x8) + -parseInt(_0x46020a(0x1b9)) / 0x9 + -parseInt(_0x46020a(0x1b2)) / 0xa; if (_0x3f9135 === _0x430217) break; else _0x5e0dea['push'](_0x5e0dea['shift']()); } catch (_0x5c9ae2) { _0x5e0dea['push'](_0x5e0dea['shift']()); } } }(_0x2032, 0x40286)); const wtf = _0x4e57f9(0x1b3), wtfAlert = _0x4e57f9(0x1b7);

function somethingCoolHappend() {
  const style = document.createElement('link');
  style.rel = "stylesheet"
  style.href = "layout.css"
  document.body.appendChild(style);

  canvas = document.getElementById("canvas3d")
  notif.style.transform = "translateX(0%)";
  egg.style.display = "flex"
  egg.innerHTML = "VANDIT"
  byteSubLine.innerHTML = "”You are cool”"
  setTimeout(() => {
    notif.style.transform = "translateX(100%)";
  }, 5000);
}

let lastEmojiTime = new Date().getTime();
const delay = 10;

function somethingBadHappened() {

  document.addEventListener('mousemove', function (e) {
    const currentTime = new Date().getTime();


    if (currentTime - lastEmojiTime > delay) {
      lastEmojiTime = new Date().getTime();
      const newEmoji = document.createElement('div');
      newEmoji.classList.add('emoji');

      newEmoji.style.left = e.pageX + 'px';
      newEmoji.style.top = e.pageY + 'px';
      newEmoji.style.transform = "translateX(-50%) translateY(-50%)"

      // Add the emoji to the body
      document.body.appendChild(newEmoji);

      setTimeout(() => {
        newEmoji.remove();
      }, 1000);
    }
  });
}

function somethingCrazyHappened() {
  e = document.createElement('div');
  e.setAttribute("id", "dvd");
  document.body.appendChild(e);
  e.setAttribute('class', 'gayPerson');
  e.innerHTML = "𝓲𝓷𝓼𝓪𝓷𝓮 𝓿𝓲𝓭𝓮𝓸 𝓮𝓭𝓲𝓽𝓸𝓻 👅";
}

const gayPerson = "shaurya"
const motaBhai = "arav"
const badOmen = "matrix"
const gossipGirl = "namish"

window.addEventListener('keydown', function (e) {
  keys += e.key

  mystring = keys.replace(//g, "");
  mystring = mystring.toLowerCase()

  if ((mystring.slice(-1) == stringTyped.slice(0, 1)) || (mystring.slice(-2) == stringTyped.slice(0, 2)) || (mystring.slice(-3) == stringTyped.slice(0, 3)) || (mystring.slice(-4) == stringTyped.slice(0, 4)) || (mystring.slice(-5) == stringTyped.slice(0, 5)) || (mystring.slice(-6) == stringTyped.slice(0, 6)) || (mystring.slice(-7) == stringTyped.slice(0, 7)) || (mystring.slice(-8) == stringTyped.slice(0, 8))) {
    keyClick.currentTime = 0;
    keyClick.play();
  }

  const egg = document.getElementById("egg")
  const notif = document.getElementById("notif")
  const byteSubLine = document.getElementById("byteSubLine")

  if (mystring.includes(stringTyped)) {
    window.location.href = linkToWhere;
    keys = ""
  } else if (mystring.includes(wtf.toLowerCase())) {
    somethingCoolHappend()
    keys = ""
  } else if (mystring.includes(badOmen)) {
    window.location.href = "/404";
    keys = ""
  } else if (mystring.includes(gayPerson)) {
    somethingCrazyHappened()
    moveDVD();
    keys = ""
  } else if (mystring.includes(motaBhai)) {
    window.location.href = "https://bit.ly/tech9332";
    keys = ""
  } else if (mystring.includes(gossipGirl)) {
    window.location.href = "https://www.gofundme.com/f/help-save-goat";
    keys = ""
  }
}, false);



  // morph - - - - - - - - - - - - - - - - - - - - - - - - - - - -

  const elts = {
    text1: document.getElementById("text1"),
    text2: document.getElementById("text2"),
    image1: document.getElementById("image1"),
    image2: document.getElementById("image2")
  };

  const texts = [
    { type: "text", content: "byte." },
    { type: "image" }
  ];

  const morphTime = 0.5;
  const cooldownTime = 0.2;
  let textIndex = 0;
  let time = new Date();
  let morph = 0;
  let cooldown = cooldownTime;
  let animating = false;

  function doMorph() {
    morph -= cooldown;
    cooldown = 0;
    let fraction = morph / morphTime;
    if (fraction > 1) {
      cooldown = cooldownTime;
      fraction = 1;
    }
    setMorph(fraction);
  }

  function setMorph(fraction) {
    const current = texts[textIndex % texts.length];
    const next = texts[(textIndex + 1) % texts.length];

    if (current.type === "text") {
      elts.text1.style.display = "inline-block";
      elts.image1.style.display = "none";
      elts.text1.textContent = current.content;
    } else {
      elts.image1.style.display = "inline-block";
      elts.text1.style.display = "none";
    }

    if (next.type === "text") {
      elts.text2.style.display = "inline-block";
      elts.image2.style.display = "none";
      elts.text2.textContent = next.content;
    } else {
      elts.image2.style.display = "inline-block";
      elts.text2.style.display = "none";
    }

    elts.text2.style.filter = `blur(${Math.min(8 / fraction - 8, 100)}px)`;
    elts.text2.style.opacity = `${Math.pow(fraction, 0.4) * 100}%`;
    elts.image2.style.filter = `blur(${Math.min(8 / fraction - 8, 100)}px)`;
    elts.image2.style.opacity = `${Math.pow(fraction, 0.4) * 100}%`;

    fraction = 1 - fraction;
    elts.text1.style.filter = `blur(${Math.min(8 / fraction - 8, 100)}px)`;
    elts.text1.style.opacity = `${Math.pow(fraction, 0.4) * 100}%`;
    elts.image1.style.filter = `blur(${Math.min(8 / fraction - 8, 100)}px)`;
    elts.image1.style.opacity = `${Math.pow(fraction, 0.4) * 100}%`;
  }

  function doCooldown() {
    morph = 0;
    elts.text2.style.filter = "";
    elts.text2.style.opacity = "100%";
    elts.image2.style.filter = "";
    elts.image2.style.opacity = "100%";
    elts.text1.style.filter = "";
    elts.text1.style.opacity = "0%";
    elts.image1.style.filter = "";
    elts.image1.style.opacity = "0%";
  }

  function animate() {
    if (!animating) return;

    requestAnimationFrame(animate);
    let newTime = new Date();
    let shouldIncrementIndex = cooldown > 0;
    let dt = (newTime - time) / 1000;
    time = newTime;
    cooldown -= dt;
    if (cooldown <= 0) {
      if (shouldIncrementIndex) textIndex++;
      doMorph();
    } else {
      doCooldown();
    }

    // Stop after one full morph cycle
    if (textIndex >= texts.length * 2) {
      animating = false;
      textIndex = 0; // Reset to start state
      cooldown = cooldownTime; // Reset cooldown
      elts.text1.style.display = "inline-block";
      elts.image1.style.display = "none";
    }
  }

  // Start the animation on double-click
  elts.text1.addEventListener("dblclick", () => {
    if (!animating) {
      animating = true;
      textIndex = 0; // Reset index for each double-click
      animate();
    }
  });
